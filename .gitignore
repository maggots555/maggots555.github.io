# El CV se queda en tu computadora. Git no lo sube a GitHub
# (ni el HTML, ni el PDF, ni el script que lo genera).
cv/

# Dependencias de Node (se reinstalan con npm install)
node_modules/

# Resultado del build de Astro (se genera con npm run build)
dist/

# Caché interna de Astro
.astro/
