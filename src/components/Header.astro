---
/**
 * Cabecera fija, casi transparente.
 * El folio de la derecha es la fecha de hoy (detalle de créditos, no un número de orden)
 * más el clima de Nicolás Romero. data-lat / data-lon los lee el script del navegador.
 *
 * compact: = desde 520px, desk: = desde 860px (ver @theme en global.css).
 */
import { sitio } from "../data/sitio";

const enlaces = [
  { href: "#inicio", label: "Inicio" },
  { href: "#sobre-mi", label: "Sobre mí" },
  { href: "#sigma", label: "SIGMA" },
  { href: "#proyectos", label: "Proyectos" },
  { href: "#contacto", label: "Contacto" },
];
---

<header class="mast">
  <div
    class="wrap grid grid-cols-1 items-center gap-5 py-3.5 compact:grid-cols-[1fr_auto] desk:grid-cols-[auto_1fr_auto]"
  >
    <a class="flex items-center gap-3 no-underline" href="#inicio">
      <span class="brand-mark" aria-hidden="true">{sitio.iniciales}</span>
      <span class="brand-copy flex flex-col leading-[1.15]">
        <strong>{sitio.nombreCorto}</strong>
        <small>@{sitio.handle}</small>
      </span>
    </a>

    <nav
      class="nav col-span-full flex flex-wrap justify-start gap-x-[1.25rem] gap-y-[0.35rem] font-body text-[0.92rem] desk:col-span-1 desk:justify-center"
      aria-label="Secciones"
    >
      {enlaces.map((enlace) => (
        <a href={enlace.href}>{enlace.label}</a>
      ))}
    </nav>

    <p
      class="folio m-0 hidden text-right font-body text-[0.82rem] italic text-crema compact:block"
      data-lat={sitio.latitud}
      data-lon={sitio.longitud}
    >
      <span>Hoy</span>
      <time id="folio-fecha" class="block not-italic font-medium text-papel" datetime="2026-09-10">
        10 de Septiembre del 2026
      </time>
      <!-- Vacío hasta que llegue Open-Meteo. Si falla la red, se queda oculto. -->
      <span id="folio-clima" class="folio-clima" hidden></span>
    </p>
  </div>
  <div class="scroll-ink" aria-hidden="true"><span id="scroll-ink-bar"></span></div>
</header>
